services:
  - type: web
    name: ai-test-automation-demo-app
    env: python
    rootDir: demo-app
    buildCommand: "pip install -r requirements.txt"
    startCommand: "gunicorn app:app"
    plan: free
